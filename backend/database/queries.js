//  Callback
//  Promise
//  Async Await

// SHOW DATABASES; whatsapp

// CREATE DATABASE future_stack;

// USE DATABASE future_stack;

// SHOW tables;

//  // whatsapp images, whatsapp audios, whatsapp videos

// CREATE TABLE Users (
//     UserId int PRIMARY KEY,
//     FirstName varchar(50) NOT NULL,
//     PLACE varchar(50)
// );

// Column name, data type,
// CREATE TABLE students (
//     id int PRIMARY KEY,
//     student_name varchar(50) NOT NULL,
//     student_address varchar(50),
//     bus_number int default 17,
//     created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
//     updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
// );

// DESC Users; // Table structure

// SELECT * FROM Users; // show data

// INSERT INTO Users VALUES(1,"Indhu", "Alangulam"); // add data

// INSERT INTO students (id,student_name,student_address)VALUES(1,"Indhu", "Alangulam"); // add data

// SELECT UserId, FirstName FROM Users; // Select only required fields

// SELECT UserId as id, FirstName as name FROM Users; // Rename fields

// INSERT INTO students (id,student_name,student_address,bus_number)VALUES(4,"Mercy", "Alangulam",18),(5,"Mans","Alm",17),(6,"Vidhya","UK",18) ;




